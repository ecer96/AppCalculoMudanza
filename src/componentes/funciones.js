import React from 'react'
import { useState } from "react";

const funciones = () => {
  return (
  
    const[valor,setvalor]=useState(0);

    function sumar(numero) {
    
    if(numero===0){
        setvalor(1)
        setvalor(numero+1);
        } else setvalor(numero+1);
    }

    function restar(numero) {
    if(numero===0){
        setvalor(0)
    }else{setvalor(numero-1)}
  };
    
    <div></div>
  )
}

export default funciones